pull_request_rules:
  - name: render probe via comment
    conditions:
      - label=poc-merge
    actions:
      comment:
        message: |
          MERGIFY-RENDER-PROBE
          expr={{ 7*7 }}
          titlelen={{ title | length }}
          upper={{ "abc" | upper }}
